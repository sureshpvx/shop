<div class="p-6 max-w-lg mx-auto bg-white rounded-xl shadow-2xl space-y-6 mt-10 border border-gray-100">
  <h1 class="font-bold text-4xl text-gray-800">Home#index</h1>

  <% if @user_name %>
    <p class="text-xl text-gray-600">
      Welcome, <span class="font-extrabold text-indigo-700"><%= @user_name %></span>!
    </p>

    <!--
      This is the logout link.
      It points to the '/logout' path and uses the DELETE method,
      which triggers your SessionsController#destroy action.
    -->
    <%= link_to 'Log Out', logout_path,
                data: { turbo_method: :delete },
                class: "inline-block px-5 py-2 bg-red-600 text-white font-semibold rounded-lg shadow-md hover:bg-red-700 transition duration-200 ease-in-out transform hover:scale-[1.02]" %>

  <% else %>
    <p class="text-xl text-gray-600">
      You are not logged in.
    </p>
    <!-- Placeholder links if you have them -->
    <%= link_to 'Log In', login_path, class: "text-indigo-600 hover:text-indigo-800 font-medium" %> |
    <%= link_to 'Sign Up', signup_path, class: "text-indigo-600 hover:text-indigo-800 font-medium" %>
  <% end %>

  <p class="text-sm text-gray-500 pt-4 border-t mt-4">Find me in app/views/home/index.html.erb</p>
</div>
